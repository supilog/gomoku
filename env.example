# ===========================================
# 五目ならべ 本番環境用 環境変数テンプレート
# ===========================================
# 本番デプロイ前に .env としてコピーし、値を設定してください。
# cp env.example .env
# ===========================================

# 【必須】セッション・CSRF用の秘密鍵（本番では必ず変更）
# 生成例: openssl rand -hex 32
SECRET_KEY=

# 【必須】本番モード（production にすると Secure Cookie 等が有効になる）
FLASK_ENV=production

# データベース接続（本番では PostgreSQL 等を推奨。SQLite の例）
# SQLite: sqlite:////app/data/gomoku.db
# PostgreSQL: postgresql://user:password@host:5432/gomoku
SQLALCHEMY_DATABASE_URI=sqlite:////app/data/gomoku.db

# （任意）デバッグモード。本番では必ず 0 または未設定
FLASK_DEBUG=0
